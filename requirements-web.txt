# Web-deploy dependencies for the single web UI:
#   * Wakanda (server.py, FastAPI) — what the Render blueprint runs (uvicorn).
# Leo (the old Flask app_web.py) was retired on 2026-07-28, so flask/gunicorn are gone.
# (The full requirements.txt additionally pulls Streamlit for the original app.py.)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
matplotlib>=3.8          # server.py imports drift_sentiment.plotting (box/GEX PNGs)
requests>=2.32
python-dotenv>=1.0
python-multipart>=0.0.9   # FastAPI request.form() en el login lo necesita
