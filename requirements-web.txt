# Lean dependencies for the Wakanda Forever web deploy (Render) — the FastAPI app
# in server.py. NOT the full requirements.txt (which also pulls Streamlit, etc.).
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
requests>=2.32
python-dotenv>=1.0
matplotlib>=3.8          # server.py imports drift_sentiment.plotting (box/GEX PNGs)
